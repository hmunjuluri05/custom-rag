# API Gateway Configuration
# Single API key for all models (embeddings and LLMs)
API_KEY=your_api_key_here

# Optional: Gateway Base URL (customize for your enterprise setup)
# If not set, will use provider-specific URLs (OpenAI/Google direct)
# For API Gateway: https://api.your-gateway.com
# BASE_URL=https://api.your-gateway.company.com

# Default Model Configuration
DEFAULT_LLM_PROVIDER=openai
DEFAULT_LLM_MODEL=gpt-4
DEFAULT_EMBEDDING_MODEL=text-embedding-3-small

# Optional: SSL Verification (set to true on corporate networks with SSL issues)
# BYPASS_SSL_VERIFICATION=false

# Optional: Logging Configuration
# LOG_LEVEL=INFO

# Optional: Upload Configuration
# MAX_FILE_SIZE_MB=50
# UPLOAD_DIR=./uploads
